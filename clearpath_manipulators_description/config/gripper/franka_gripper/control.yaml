controller_manager:
  ros__parameters:
    # Match Gazebo physics step (0.01 s => 100 Hz) to avoid "update period" warnings
    update_rate: 100  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster
